const producto = {
  nombre: "Monitor 24 Pulgadas",
  precio: 300,
  disponible: true,
};
// Retorna array con los keys del objeto
console.log(Object.keys(producto));
// valores del objeto
console.log(Object.values(producto));

// retorna en pares de valor
console.log(Object.entries(producto));
