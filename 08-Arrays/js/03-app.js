const meses = ["Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio"];

console.table(meses[0]);
console.table(meses[1]);

// Cuanto mide el arreglo, inicia en 1
console.log(meses.length);

// Recorrer Arrays

for (let i = 0; i < meses.length; i++) {
  console.log(meses[i]);
}
